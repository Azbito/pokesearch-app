import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    borderWidth: 2,
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 8,
    gap: 16,
    borderRadius: 16,
    justifyContent: 'center'
  }
})
